node_modules

# Carried byte for byte from releasetools/actions; formatting it here would
# make the copies differ.
plugins/release-notes/bin/releasetools-config.cjs

# Generated by scripts/sync-catalogs.mjs, which writes its own formatting.
# Prettier reformatting them is how `npm run check` fails on a clean tree.
plugins/*/plugin.json
.claude-plugin/marketplace.json
.agents/plugins/marketplace.json
