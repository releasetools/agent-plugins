# releasetools agent plugins

The official Claude Code and Codex plugin marketplace for
[releasetools](https://github.com/releasetools).

The catalogs are intentionally empty until the first plugin publication:

- Claude Code reads `.claude-plugin/marketplace.json`.
- Codex reads `.agents/plugins/marketplace.json`.
